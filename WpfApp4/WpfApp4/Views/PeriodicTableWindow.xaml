<Window x:Class="ChemistryIS.Views.PeriodicTableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ChemistryIS"
        xmlns:converters="clr-namespace:ChemistryIS.Converters"
        xmlns:vm="clr-namespace:ChemistryIS.ViewModels"
        Title="Периодическая система химических элементов Д.И. Менделеева" 
        Height="900" Width="1500"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5">

    <Window.Resources>
        <converters:CategoryToColorConverter x:Key="CategoryToColorConverter"/>

        <Style x:Key="ElementButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{Binding Category, Converter={StaticResource CategoryToColorConverter}}"
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid Margin="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding AtomicNumber}" FontSize="9" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="2,1,0,0" FontWeight="SemiBold"/>
                                <TextBlock Grid.Row="1" Text="{Binding Symbol}" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Grid.Row="2" Text="{Binding NameRu}" FontSize="8" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Bottom" TextTrimming="CharacterEllipsis" MaxWidth="45"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderThickness" Value="3"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFD700"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#0078D7" Opacity="0.6" BlurRadius="10" ShadowDepth="2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Периодическая система химических элементов Д.И. Менделеева" 
                   FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10" HorizontalAlignment="Center"/>

        <WrapPanel Grid.Row="1" Margin="0,0,0,15" HorizontalAlignment="Center">
            <Border Background="#FF6B6B" CornerRadius="3" Padding="5,2" Margin="2"><TextBlock Text="Щелочные" Foreground="White" FontSize="10"/></Border>
            <Border Background="#FF9F43" CornerRadius="3" Padding="5,2" Margin="2"><TextBlock Text="Щелочнозем." Foreground="White" FontSize="10"/></Border>
            <Border Background="#54A0FF" CornerRadius="3" Padding="5,2" Margin="2"><TextBlock Text="Переходные" Foreground="White" FontSize="10"/></Border>
            <Border Background="#00D2D3" CornerRadius="3" Padding="5,2" Margin="2"><TextBlock Text="Постперех." Foreground="White" FontSize="10"/></Border>
            <Border Background="#5F27CD" CornerRadius="3" Padding="5,2" Margin="2"><TextBlock Text="Металлоиды" Foreground="White" FontSize="10"/></Border>
            <Border Background="#FF9FF3" CornerRadius="3" Padding="5,2" Margin="2"><TextBlock Text="Неметаллы" Foreground="White" FontSize="10"/></Border>
            <Border Background="#FECA57" CornerRadius="3" Padding="5,2" Margin="2"><TextBlock Text="Галогены" Foreground="White" FontSize="10"/></Border>
            <Border Background="#1DD1A1" CornerRadius="3" Padding="5,2" Margin="2"><TextBlock Text="Благородные" Foreground="White" FontSize="10"/></Border>
            <Border Background="#8395A7" CornerRadius="3" Padding="5,2" Margin="2"><TextBlock Text="Лантаноиды" Foreground="White" FontSize="10"/></Border>
            <Border Background="#576574" CornerRadius="3" Padding="5,2" Margin="2"><TextBlock Text="Актиноиды" Foreground="White" FontSize="10"/></Border>
        </WrapPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="10">
            <ItemsControl ItemsSource="{Binding AllElements}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="55"/> 
                                <RowDefinition Height="55"/> 
                                <RowDefinition Height="55"/> 
                                <RowDefinition Height="55"/> 
                                <RowDefinition Height="55"/>
                                <RowDefinition Height="55"/> 
                                <RowDefinition Height="55"/> 
                                <RowDefinition Height="25"/> 
                                <RowDefinition Height="55"/> 
                                <RowDefinition Height="55"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Grid.Row" Value="{Binding Row}"/>
                        <Setter Property="Grid.Column" Value="{Binding Column}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding}">
                            <ContentControl.Style>
                                <Style TargetType="ContentControl">
                                    <Setter Property="ContentTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <Button Style="{StaticResource ElementButtonStyle}"
                                                        Command="{Binding DataContext.SelectElementCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                        CommandParameter="{Binding}">
                                                    <Button.ToolTip>
                                                        <StackPanel MinWidth="150">
                                                            <TextBlock Text="{Binding NameRu}" FontWeight="Bold" FontSize="14"/>
                                                            <TextBlock Text="{Binding Category}" FontStyle="Italic" Foreground="Gray"/>
                                                            <Separator/>
                                                            <TextBlock Text="{Binding ElectronConfig}" FontFamily="Consolas"/>
                                                        </StackPanel>
                                                    </Button.ToolTip>
                                                </Button>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>

                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDummy}" Value="True">
                                            <Setter Property="ContentTemplate">
                                                <Setter.Value>
                                                    <DataTemplate>
                                                        <Border Width="50" Height="50" Margin="2" Background="Transparent"/>
                                                    </DataTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ContentControl.Style>
                        </ContentControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>